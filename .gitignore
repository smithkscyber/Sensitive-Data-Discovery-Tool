# Virtual environments
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/

# Mutation testing scratch: mutmut rewrites the source in place and caches
# every mutant's verdict. Both are rebuilt by `mutmut run`.
mutants/
.mutmut-cache

# Scan output — regenerated by the tool, never committed
report.csv
report.json
output/

# Editors / OS
.vscode/
.idea/
.DS_Store
